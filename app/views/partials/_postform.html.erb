<%= form_for @post do |f| %>

    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :description %>
    <%= f.text_area :description %>

    <%= f.label :photo %>
    <%= f.text_field :photo %>

    <%= f.submit %>

<% end %>